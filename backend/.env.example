# ===========================================
# Sleep Relief Navigator - Backend Environment
# ===========================================
# Copy this file to .env and fill in the values

# ===========================================
# SERVER CONFIGURATION
# ===========================================
NODE_ENV=development
PORT=5000
API_VERSION=v1

# ===========================================
# MONGODB ATLAS
# ===========================================
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/sleeprelief?retryWrites=true&w=majority

# ===========================================
# AUTHENTICATION (REQUIRED - USE STRONG SECRETS)
# ===========================================
# JWT Secret for access tokens (minimum 256-bit)
JWT_SECRET=your-super-secret-jwt-access-token-secret-min-256-bits-here

# Access token expiry (default: 15m)
JWT_ACCESS_EXPIRY=15m

# Refresh token secret (minimum 256-bit)
JWT_REFRESH_SECRET=your-super-secret-refresh-token-secret-min-256-bits-here

# Refresh token expiry (default: 7d)
JWT_REFRESH_EXPIRY=7d

# ===========================================
# STRIPE PAYMENTS
# ===========================================
# Get from https://dashboard.stripe.com/apikeys (use test mode for development)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Webhook secret - get after creating webhook endpoint at https://dashboard.stripe.com/test/webhooks
# Use Stripe CLI for local testing: stripe listen --forward-to localhost:5000/api/v1/webhooks/stripe
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret

# Price IDs - get from https://dashboard.stripe.com/test/products
# Create products and copy their Price IDs (start with "price_")
STRIPE_PRICE_ONE_TIME=price_your_7night_reset_price_id
STRIPE_PRICE_SUBSCRIPTION=price_your_monthly_subscription_price_id

# Supported Payment Methods (configured in Stripe Dashboard)
# Default: card, upi (for India)
# UPI allows payments via Google Pay, PhonePe, Paytm, BHIM UPI, etc.

# ===========================================
# EMAIL (RESEND) - REQUIRED FOR EMAILS TO WORK
# ===========================================
# Get from https://resend.com/api-keys
# For testing, you can use Resend's test domain (no DNS verification needed)
RESEND_API_KEY=re_your_resend_api_key

# OPTION 1: Use Resend's free test domain (no DNS setup)
# Go to https://resend.com/domains - look for "resendmail.com" domain
# Use the test subdomain they provide (looks like: randomstring@resend.dev)
EMAIL_FROM=your_test_subdomain@resend.dev

# OPTION 2: Use your own domain (requires DNS verification)
# Add your domain in Resend dashboard, add DNS records, then use:
# EMAIL_FROM=hello@yourdomain.com

EMAIL_FROM_NAME=Sleep Relief Navigator

# ===========================================
# UPSTASH REDIS (OPTIONAL - FOR RATE LIMITING)
# ===========================================
# Get from https://console.upstash.com
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_token

# ===========================================
# SENTRY ERROR TRACKING (OPTIONAL)
# ===========================================
# Get from https://sentry.io/settings
SENTRY_DSN=https://xxx@sentry.io/xxxxx

# ===========================================
# APPLICATION URLS
# ===========================================
APP_URL=http://localhost:5000
FRONTEND_URL=http://localhost:5173

# ===========================================
# RATE LIMITING
# ===========================================
# Enable Redis-based rate limiting (set to 'true' if Upstash configured)
USE_REDIS_RATE_LIMIT=false

# ===========================================
# LOGGING
# ===========================================
# Log level: fatal, error, warn, info, debug, trace
LOG_LEVEL=info

# ===========================================
# SECURITY
# ===========================================
# Allowed CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Enable request logging
ENABLE_REQUEST_LOGGING=true